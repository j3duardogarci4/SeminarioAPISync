

using Microsoft.EntityFrameworkCore;

namespace SeminarioAPISync.Context
{
    public class EventContext: DbContext 
        private readonly IConfiguration _configuration;
        public EventContext(IConfiguration configuration, )

    {
    }
}
